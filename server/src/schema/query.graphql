# import * from 'flight.graphql'
# import * from 'airport.graphql'

type Query {
  "Retrieve all flights"
  flights(input: FlightsInput!): [Flight!]

  "Retrieve a single flight"
  flight(input: FlightInput!): Flight

  "Retrieve all airports"
  airports(input: AirportsInput!): [Airport]

  "Retrieve a single airports"
  airport(input: AirportInput!): Airport
}
